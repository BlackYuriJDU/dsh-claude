/** A "coming soon" nav placeholder: the shell renders the row disabled and
 * never selects it, so the occupant renders nothing. Kept as a component so
 * the section slot's contract (an occupant per entry) stays uniform. */
export function SoonSection() {
  return null
}
